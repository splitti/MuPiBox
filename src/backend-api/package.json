{
  "name": "mupibox-backend-api",
  "version": "1.0.0",
  "author": "Mupi Team",
  "homepage": "https://github.com/splitti/mupibox",
  "scripts": {
    "serve": "NODE_ENV=development node src/server.js",
    "build": "esbuild src/server.js --bundle --minify --platform=node --target=node22 --outfile=../deploy/server.js",
    "lint": "npx @biomejs/biome check .",
    "lint:fix": "npx @biomejs/biome check --fix ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "jsonfile": "^6.1.0",
    "request": "^2.88.2",
    "spotify-web-api-node": "^5.0.2",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {},
  "description": "API backend server for the MupiBox.",
  "private": true
}
